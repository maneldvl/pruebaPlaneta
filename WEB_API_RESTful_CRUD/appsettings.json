{
  ///
  ///Manel 01/2021
  ///This is local server running on my PC Server=(LocalDB)\\ProjectsV12 if your local server is different change this string (LocalDB)\\ProjectsV12 for your valid connection
  ///For check local server open cmd window on system and input these commands depend if do you want to do:
  /// SQLLocalDb info for shown instances creates on local server
  /// SqlLocalDB info <Instance> shown status <Instance>
  /// SqlLocalDB start <Instance> start <Instance> if is stopped
  /// SqlLocalDB stop <Instance> turn to stopped the <Instance>

  "ConnectionStrings": {
    "ProductsDbConnectionString": "Server=(LocalDB)\\ProjectsV12;Database=DBProducts;Trusted_Connection=True;MultipleActiveResultSets=true"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning"
    }
  },
  "AllowedHosts": "*"
}
